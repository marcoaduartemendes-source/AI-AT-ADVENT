.env
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/
venv/
.venv/
env/
.env.*
!.env.example
*.log
.DS_Store
# Repo-root /data/ is the runtime SQLite cache; don't commit it.
# Anchored with a leading slash so `src/backtests/data/` (the
# Polygon/FMP API wrapper modules) is NOT excluded by this rule.
/data/
*.db
